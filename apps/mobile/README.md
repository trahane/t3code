# Harbordex Mobile Workspace

This workspace hosts Harbordex-owned mobile integration code.

- Uses native t3 WebSocket RPC contracts directly.
- Does not use a bridge compatibility protocol.
- Implements pairing credential exchange, bearer auth, ws token issuance, and `/ws` RPC flows.

Harbordex is a fork of t3code.
