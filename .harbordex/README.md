# Harbordex Fork Guardrails

- `.harbordex/upstream-allowlist.txt` defines intentional upstream-core patch coverage.
- `scripts/verify-upstream-boundary.mjs` enforces boundaries in CI and local checks.

Harbordex is a fork of t3code.
